class LayoutConstants {
  /// Screens wider than this get the master-detail / rail layout.
  static const double desktopBreakpoint = 700;

  /// Max width for forms and content cards on wide screens.
  static const double formMaxWidth = 600;

  /// Width of the persistent class-list sidebar on desktop.
  static const double sidebarWidth = 320;
}
